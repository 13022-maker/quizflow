import { ReactionLanding } from '@/features/reactions/ReactionLanding';

export const metadata = {
  title: '課堂回饋 | QuizFlow',
};

export default function ReactionsLandingPage() {
  return <ReactionLanding />;
}
